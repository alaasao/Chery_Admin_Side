import React from 'react'

const Loading = () => {
  return (
    <div className="absolute top-0 z-[10] flex items-center justify-center w-screen h-screen" > <div className="loader">Loading...</div></div>
  )
}

export default Loading
